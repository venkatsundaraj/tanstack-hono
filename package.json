{
  "name": "tanstack+bun",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=!web --filter=!docs",
    "dev": "turbo run dev --filter=!web --filter=!docs",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.7.5",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "hono": "^4.11.5"
  }
}
