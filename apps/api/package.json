{
  "name": "api",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "local-dev": "bun run --port 3001 --hot src/index.ts ",
    "build": "echo 'No build step required'",
    "start": "bun run src/index.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20260124.0",
    "better-auth": "^1.4.17",
    "hono": "^4.11.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.60.0"
  }
}
